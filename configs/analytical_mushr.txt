  {
  "env_name": "CarLikeEnv_VG",

  # Environment Setup Configuration:
  "dt": 0.1,
  "max_steps": 150,
  "prop_steps": 3,
  "velocity_limits": [-0.3, 1],
  "acceleration_limits": [-0.3, 0.3],
  "steering_angle_limits": [-0.523, 0.523],
  "env_limit": 10.0,

  # Goal Configuration
  "distance_threshold": [0.5, 0.25, 0.3],
  "loss_mode": "combine_all_dist", # Possible Values: "combine_pos_angle_dist", "combine_all_dist", "individual"
  "goal_limit": [
    ["-env_limit", "env_limit"],
    ["-env_limit", "env_limit"],
    ["-pi", "pi"],
    ["low_vel", "high_vel"]
  ],
  "start_limit": [
    ["-env_limit", "env_limit"],
    ["-env_limit", "env_limit"],
    ["-pi", "pi"],
    ["low_vel", "high_vel"],
    [0.0,0.0],  # ["low_steer", "high_steer"]
  ],


  # Training Configuration:
  "num_steps": 3e6,
  "alg": "HER_SAC",
  "seed": 0, # Possible Values: Numerical Values, "random",
  "model_uses_alt_env": True,
  "model_env_config": "analytical_mushr_zero_goal",

  # geometry Configuration
  "geometry": True,
  "geometry_yml": "bar",

  # edge_enhancement config
  "edge_enhancement": False,

    # Roadmap Configuration
  "epsilon": 10,
  "max_rollout_steps_in_construction": 150
}
